# Production Configuration

Place only non-secret production configuration templates here. Production credentials, signing material, Firebase secrets, and tokens must be injected securely by the release environment.