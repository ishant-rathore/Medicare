# Android Deployment

Android release documentation belongs here.

Required release evidence includes the signed release artifact, version/build number, manifest/permission review, notification/alarm verification, Firebase configuration verification, and device smoke-test results.

The Flutter-generated `android/` project is a separate required platform directory for an actual Android build and must be restored/generated with the approved Flutter SDK if it is absent.