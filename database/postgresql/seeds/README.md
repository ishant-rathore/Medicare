# PostgreSQL Seed Data

Development/test-only seed data belongs here.

Rules:

- Use synthetic records only.
- Never use real patient or community participant information.
- Never run seed scripts automatically as part of production migration/deployment.
- Keep seeded data compatible with the current Prisma schema and application tests.
- Production/demo datasets must be created through approved operational procedures, not committed fixture data.
