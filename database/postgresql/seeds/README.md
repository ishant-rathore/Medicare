# PostgreSQL Development/Test Seeds

This directory is reserved for synthetic development and test seed data.

Rules:

- Use only obviously synthetic records.
- Never use real patient, caregiver, participant, or community data.
- Never run development/test seeds automatically in production.
- Keep seed data deterministic where practical so integration tests remain reproducible.

Production data must be provisioned through the approved deployment process and environment configuration.
